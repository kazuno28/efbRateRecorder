<% rate_graph_comments.each do |rate_graph_comment| %>
  <% rate_graph_comment_user = rate_graph_comment.user %>
  <tr>
  	<td>
  		<%= link_to user_path(rate_graph_comment_user), class: "user_#{rate_graph_comment_user.id}" do %>
  		  <% if user.image.attached? %>
          <%= image_tag user.image.variant(resize:"50x50") %>
        <% else %>
          <%= image_tag 'PF-noimage.png' %>
        <% end %><br>
    		<%= rate_graph_comment_user.name %>
  		<% end %>
  	</td>
  	<td><%= rate_graph_comment.comment %></td>
  	<td><%= render 'favorites/favorite-btn', user: @user, rate_graph_comment: rate_graph_comment %></td>
  	<% if current_user == rate_graph_comment_user %>
    	<td><%= link_to '削除', user_rate_graph_comment_path(rate_graph_comment.user_id, rate_graph_comment.id), class: 'btn-sm btn-danger', method: :delete, remote: true %></td>
  	<% end %>
  </tr>
<% end %>